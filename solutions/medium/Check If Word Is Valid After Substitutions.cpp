// Title: Check If Word Is Valid After Substitutions
            // Difficulty: Medium
            // Language: C++
            // Link: https://leetcode.com/problems/check-if-word-is-valid-after-substitutions/

#include <string>
#include <vector>

using namespace std;

class Solution {
public:
    bool isValid(string s) {
        for(char c:s){
            if(c=='c'){
        }
            }else result+=c;
    }
        string result="";
                if(result.back()=='b')result.pop_back();
                else return false;
                if(result.back()=='a')result.pop_back();
                else return false;
        if(result.size()==0)return true;
                if(result.length()<2)return false;
        return false;
};
