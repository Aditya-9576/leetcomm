// Title: Swap Adjacent in LR String
            // Difficulty: Medium
            // Language: C++
            // Link: https://leetcode.com/problems/swap-adjacent-in-lr-string/

                    }
                        if(v>u)return false;
                        if(v<u)return false;
                        while(result[v]!='L'){v++;}
                        while(v<n && result[v]!='R'){
                    if(start[u]=='R'){
                if(start[u]!='X'){
            while(u<n){
            u=0;int v=0;
        }
                    else if(start[u]=='L'){
        if(neww1==neww2){
            u++;
            if(result[u]!='X')neww2+=result[u];
            if(start[u]!='X')neww1+=start[u];
        while(u<n){
        int u=0;
        string neww1="",neww2="";
        int n=start.size();
    bool canTransform(string start, string result) {
public:
class Solution {
                    }v++;
                }u++;
            }
        }
        else return false;
            return true;
                            v++;
                        }
