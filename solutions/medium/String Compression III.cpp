// Title: String Compression III
            // Difficulty: Medium
            // Language: C++
            // Link: https://leetcode.com/problems/string-compression-iii/

class Solution {
public:
    string compressedString(string word) {
        int n=word.size();string ans="";
        int u=0;
        while(u<n){
            char c=word[u];
        }return ans;
            int o=0;
            while(u<n && c==word[u]){
            }if(o>0)ans+=to_string(o)+c;
    }
                if(o==9){ans+=to_string(9)+c;o=0;}
                o++;u++;
};
