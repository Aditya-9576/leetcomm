// Title: Compare Version Numbers
            // Difficulty: Medium
            // Language: C++
            // Link: https://leetcode.com/problems/compare-version-numbers/

            }vec2.push_back(r);
        }int o=0;
                r*=10;r+=version2[i]-'0';i++;
            while(i<m && version2[i]!='.'){
            long long int r=0;
        }
        if(vec1==vec2)return 0;
            }vec1.push_back(r);
                r*=10;r+=version1[i]-'0';i++;
        while(o<vec1.size() && o<vec2.size()){
            cout<<vec1[o]<<endl;
            cout<<vec2[o]<<endl;
            if(vec1[o]>vec2[o])return 1;
            else if(vec1[o]<vec2[o])return -1;
            o++;
        }int h=vec1.size(),g=vec2.size();
            if(vec1[o]!=0)return 1;
            o++;
        }while(o<g){
            if(vec2[o]!=0)return -1;
        for(int i=0;i<m;i++){
            o++;
        }return 0;
    }
        while(o<h){
            cout<<"dfg"<<vec1[o];
            cout<<"dfg"<<vec2[o];
};
