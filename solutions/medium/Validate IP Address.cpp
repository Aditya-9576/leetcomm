// Title: Validate IP Address
            // Difficulty: Medium
            // Language: C++
            // Link: https://leetcode.com/problems/validate-ip-address/

                    }
                        return no;
                        cout<<"hi";
                    else {
                    }
                        o+=y-'0';
                    if(y>='0' && y<='9'){
                    o*=10;
                for(char y:s){
                >3)return no;
                    s+=queryIP[u];u++;
                }int o=0;cout<<s;if(s.size()>1 && s[0]=='0')return no;if(s.size()==0)return no;if(s.size()
            ans+="IPv4";
            int u=0,k=0;
            while(u<n){
                string s="";
                while(u<n && queryIP[u]!='.'){
        if(queryIP[n-1]=='.'||queryIP[0]=='.'||queryIP[n-1]==':'||queryIP[0]==':')return no;
        if(isfour){
        int n=queryIP.size();bool isfour=true;string no="Neither";if(queryIP=="")return no;
        for(char c:queryIP){
            if(c==':'){
                isfour=false;break;
            }if(c=='.')break;
        }string ans="";
class Solution {
public:
    string validIPAddress(string queryIP) {
